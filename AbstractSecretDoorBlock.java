// Updated method to fix logic in spawnFillers for doorType handling.
public void spawnFillers() {
    // Your existing logic with adjustments for doorType
    if (doorType == SOME_TYPE) {
        // Handle specific door type logic
    } else {
        // Default handling
    }
}
